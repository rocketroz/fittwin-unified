describe('Scenario 1: Shopper signup → try-on → checkout', () => {
  it('walks through signup, avatar, try-on, and checkout flow (currently failing until implemented)', () => {
    expect(true).toBe(false);
  });
});
