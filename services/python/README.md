# Python Services

Place Python-based microservices here. Each service should expose its own
requirements file and entrypoint, and keep virtual environment artifacts out of
version control.

- `measurement/` – incoming FastAPI implementation for MediaPipe-driven
  measurement + recommendation APIs.
